## 10 – Authentication & User Accounts

- **Goals**:
  - Implement secure login/register flows with email + OAuth (Google).
- **Tasks**:
  - Build responsive login and register pages with password visibility toggle.
  - Integrate auth (e.g. NextAuth or custom) connected to Node.js backend + MongoDB.
  - Ensure SEO safety (noindex, clean URLs, minimal crawlable content where needed).
  - Add basic profile settings area for authors/admins.
- **Deliverable**:
  - Working auth system with protected admin routes and author recognition on posts.


