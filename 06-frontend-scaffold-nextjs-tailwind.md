## 06 – Frontend Scaffold (Next.js + Tailwind)

- **Goals**:
  - Initialize the Next.js app (App Router) with Tailwind and base layout.
- **Tasks**:
  - Create Next.js app (`frontend`), configure TypeScript and Tailwind.
  - Set up global layout with semantic structure (`<header>`, `<main>`, `<footer>`).
  - Configure base Tailwind theme (colors, fonts, container, shadows).
  - Add basic pages for `/`, `/category/[slug]`, `/blog/[slug]`, `/search`.
- **Deliverable**:
  - Running Next.js dev server showing placeholder pages and shared layout.


